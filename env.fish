# Use this file to set environment variables that are used to identify
# the system and it's parts to various helper scripts and functions
set -gx REPOSITORY_DIR "$HOME/dev"
set -gx EDITOR /opt/homebrew/bin/nvim
