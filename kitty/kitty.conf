# Minimal kitty config: let tmux handle sessions, panes, tabs, and scrollback.
# font_family Monoid Nerd Font
# bold_font auto
# italic_font auto
# bold_italic_font auto
font_size 16.0

include current-theme.conf

# Keep kitty visually out of the way.
tab_bar_style hidden
window_padding_width 0
confirm_os_window_close 0
enable_audio_bell no
background_opacity 0.3
background_blur 0

disable_ligatures never

# Do not expose kitty's tab/window/pane keybindings. Re-add the default
# non-multiplexing shortcuts below.
kitty_mod ctrl+shift
clear_all_shortcuts yes

# Clipboard.
map kitty_mod+c copy_to_clipboard
map kitty_mod+v paste_from_clipboard
map kitty_mod+s paste_from_selection
map kitty_mod+o pass_selection_to_program

# macOS system shortcuts are removed by clear_all_shortcuts, so restore the
# expected Command-key behavior explicitly instead of passing it to the shell.
map cmd+c copy_to_clipboard
map cmd+v paste_from_clipboard
map cmd+q quit

# Scrolling / scrollback.
map kitty_mod+up        scroll_line_up
map kitty_mod+down      scroll_line_down
map kitty_mod+page_up   scroll_page_up
map kitty_mod+page_down scroll_page_down
map kitty_mod+home      scroll_home
map kitty_mod+end       scroll_end
map kitty_mod+h         show_scrollback

# Font sizes.
map kitty_mod+equal     change_font_size all +2.0
map kitty_mod+minus     change_font_size all -2.0
map kitty_mod+backspace change_font_size all 0

# Select and act on visible text.
map kitty_mod+e         kitten hints
map kitty_mod+p>f       kitten hints --type path --program -
map kitty_mod+p>shift+f kitten hints --type path
map kitty_mod+p>l       kitten hints --type line --program -
map kitty_mod+p>w       kitten hints --type word --program -
map kitty_mod+p>h       kitten hints --type hash --program -
map kitty_mod+p>n       kitten hints --type linenum
map kitty_mod+p>y       kitten hints --type hyperlink

# Miscellaneous non-pane/tab controls.
map kitty_mod+f11       toggle_fullscreen
map kitty_mod+f10       toggle_maximized
map kitty_mod+u         kitten unicode_input
map kitty_mod+f2        edit_config_file
map kitty_mod+a>m       set_background_opacity +0.1
map kitty_mod+a>l       set_background_opacity -0.1
map kitty_mod+a>1       set_background_opacity 1
map kitty_mod+a>d       set_background_opacity default
map kitty_mod+delete    clear_terminal reset active
map kitty_mod+f5        load_config_file
map kitty_mod+f6        debug_config

# Classic Linux selection behavior.
copy_on_select clipboard
clipboard_control write-clipboard write-primary read-clipboard read-primary



# BEGIN_KITTY_FONTS
font_family      family="Mononoki Nerd Font"
bold_font        auto
italic_font      auto
bold_italic_font auto
# END_KITTY_FONTS
